# createNotes

## What is it?

Automate creating text files for notes

## Use

Create config file:

```
#include <string>
class config
{
    public:
    std::string dirPath = "pathYouWishToSaveNotes";
}
```
